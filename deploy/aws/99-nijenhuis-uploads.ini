; Nijenhuis — allow for-sale / admin image uploads larger than PHP defaults (often 2M).
; post_max_size must be >= upload_max_filesize for multipart/form-data.
upload_max_filesize = 12M
post_max_size = 16M
