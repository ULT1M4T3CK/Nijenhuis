# Nijenhuis Botenverhuur - Python Dependencies
# Version pinning with upper bounds for security and stability.
# Minimums raised to pull in fixes for known CVEs; upper bounds relaxed to
# the current major so `pip install -U` picks up future patches.

# Core API
flask>=3.0.3,<4.0.0
flask-cors>=4.0.1,<6.0.0
requests>=2.32.3,<3.0.0
beautifulsoup4>=4.12.3,<5.0.0
pyjwt>=2.9.0,<3.0.0
cryptography>=43.0.1,<46.0.0

# Production WSGI Server
gunicorn>=22.0.0,<24.0.0
sdnotify>=0.3.2,<1.0.0


